<%= form_with(url: search_publishers_url, method: :get) do |f| %>

  <%= f.label :name, "出版社名" %>
  <%= f.text_field :name, value: @name, class: 'form-control' %> <br>

  <%= f.submit "検索", class: "btn btn-primary" %>
  
<% end %>