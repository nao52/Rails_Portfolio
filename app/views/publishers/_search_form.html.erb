<%= form_with url: search_publishers_url, method: :get, class: "search_form" do |f| %>

  <%= f.label :name, "出版社名" %>
  <%= f.text_field :name, value: @name %>

  <%= f.submit "検索", class: "button" %>
  
<% end %>