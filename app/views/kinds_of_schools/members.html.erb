<% provide(:title, @group.name) %>

<h1 class="page-title"><%= yield(:title) %></h1>

<div class="kinds_of_school_link">
  <div><%= link_to "投稿一覧", @group %></div>
  <div><%= link_to "メンバー", members_kinds_of_school_path(@group), class: "active" %></div>
</div>

<%= render 'users/user' %>