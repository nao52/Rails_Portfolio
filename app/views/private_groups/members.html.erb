<% provide(:title, @group.name) %>

<%= render 'private_groups/group_info' %>

<div class="private_group_link">
  <div><%= link_to "投稿一覧", @group %></div>
  <div><%= link_to "メンバー", members_private_group_path(@group), class: "active" %></div>
</div>

<%= render 'users/user' %>