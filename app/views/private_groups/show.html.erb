<% provide(:title, @group.name) %>
<% provide(:params_id, "private_group_id") %>

<%= render 'private_groups/group_info' %>

<div class="private_group_link">
  <div><%= link_to "投稿一覧", @group, class: "active" %></div>
  <div><%= link_to "メンバー", members_private_group_path(@group) %></div>
</div>

<%= render 'shared/post_form', params_id: "private_group_id" %>
<%= render 'shared/post' %>