<%= form_with(model: @private_group) do |f| %>
  <%= render 'shared/error_messages', model: f.object %>

  <%= f.label :name, "グループ名" %>
  <%= f.text_field :name %>

  <%= f.label :detail, "グループ詳細(任意)" %>
  <%= f.text_area :detail %>

  <%= f.submit yield(:button_text), class: "button" %>
<% end %>