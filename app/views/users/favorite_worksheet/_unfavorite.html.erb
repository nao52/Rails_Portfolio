<%= form_with(model: current_user.worksheet_favorites.find_by(worksheet: worksheet.id),
              html: { method: :delete }) do |f| %>
  <%= f.submit "お気に入り解除(#{worksheet.worksheet_favorites.count})", class: "btn btn-danger" %>
<% end %>