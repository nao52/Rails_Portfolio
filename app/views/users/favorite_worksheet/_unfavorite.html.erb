<%= form_with model: current_user.worksheet_favorites.find_by(worksheet: worksheet.id),
              html: { method: :delete }, class: "unfavorite_form" do |f| %>
  <%= f.submit "お気に入り解除(#{worksheet.worksheet_favorites.count})", class: "button" %>
<% end %>