<% provide(:title, 'ユーザー一覧') %>
<h1 class="text-center">ユーザー一覧</h1>

<%= form_with(url: search_users_url, method: :get) do |f| %>

  <%= f.label :name, "名前" %>
  <%= f.text_field :name, class: 'form-control' %> <br>

  <%= f.submit "検索", class: "btn btn-primary" %>
<% end %>

<%= render 'user' %>