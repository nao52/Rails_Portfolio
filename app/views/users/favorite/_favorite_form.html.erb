<div id="favorite_form">
<% if current_user.favorite_book?(book) %>
  <%= render 'users/favorite/unfavorite', book: book %>
<% else %>
  <%= render 'users/favorite/favorite', book: book %>
<% end %>
</div>