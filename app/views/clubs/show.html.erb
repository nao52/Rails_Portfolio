<% provide(:title, @group.name) %>

<h1 class="page-title"><%= yield(:title) %></h1>

<div class="club_link">
  <div><%= link_to "投稿一覧", @group, class: "active" %></div>
  <div><%= link_to "メンバー", members_club_path(@group) %></div>
</div>

<%= render 'shared/post_form', params_id: "club_id" %>

<%= render 'shared/post' %>