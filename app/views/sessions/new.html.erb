<% provide(:title, "ログイン") %>

<div class="login-content">
  <h2 class="page-title">日々の努力を共有しよう</h2>
  <p>素敵な学校を一緒に作りませんか？教員のためのSNSアプリケーション。</p>

  <%= form_with(url: login_path, scope: :session, class: "login-form") do |f| %>

    <div>
      <%= f.label :email, "メールアドレス" %>
      <%= f.email_field :email, class: 'form-control' %>
    </div>

    <div>
      <%= f.label :password, "パスワード" %>
      <%= f.password_field :password, class: 'form-control' %>
    </div>

    <div class="checkbox">
      <%= f.check_box :guest, {}, true, false %>
      <%= f.label :guest, "ゲストとしてログインする" %>
    </div>

    <%= f.submit "ログイン", class: "button" %>
    
  <% end %>

  <p><%= link_to "登録がお済みでない方はこちら！", signup_path %></p>
</div>