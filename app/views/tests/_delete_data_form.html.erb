<% if @names.size > 0 %>

  <%= form_with(url: test_delete_data_path) do |f| %>

    <div><%= hidden_field_tag :col,   @col %></div>
    <div><%= hidden_field_tag :row,   @row %></div>

    <%= f.submit "生徒名をクリア", class: "btn btn-danger" %>

  <% end %>
  
<% end %>
